FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /work

RUN pip install --no-cache-dir --upgrade pip \
 && pip install --no-cache-dir thread-order progress1bar thread-viewer faker

ENTRYPOINT ["tdrun"]
